---
# RHEL specific properties
rhel:
  vagrant_download_path: "{{ temporal_directory }}/vagrant_{{ vagrant_version }}.rpm"
  syslog_tag: vagrant
  log:
    file_group: adm
    stdout: "{{ log_directory }}/output.log"
    error: "{{ log_directory }}/error.log"

install_packages: []